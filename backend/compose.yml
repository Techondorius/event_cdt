version: "3"

services:
  mypy:
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - .:/src
    tty: true
    ports:
      - 8000:8000
    command: sh -c "
      cd /src && pip install pipenv &&
      echo asdf &&
      pipenv install &&
      . .venv/bin/activate &&
      pipenv run uvicorn src.main:app --reload --host 0.0.0.0"
